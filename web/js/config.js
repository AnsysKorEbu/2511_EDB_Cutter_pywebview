/**
 * Configuration for unit conversion
 * This will be updated from Python API on startup
 */
const CONFIG = {
    scale: 1000,        // Default: mm to um (will be set from Python)
    inputUnit: 'mm',    // Input unit from EDB
    outputUnit: 'um'    // Display unit
};
